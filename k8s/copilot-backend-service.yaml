
apiVersion: v1
kind: Service
metadata:
  name: copilot-backend
  annotations:
    cloud.google.com/load-balancer-type: "Internal"
spec:
  type: LoadBalancer
  selector:
    app: copilot-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
